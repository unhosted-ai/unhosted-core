.venv/
__pycache__/
*.pyc

# Training artifacts. Adapters can be large (~50–500 MB);
# never commit them. Push to HF Hub instead.
out/
data/
checkpoints/
wandb/

# OS noise.
.DS_Store

# Model weights and local eval runs — never in git (HF Hub instead).
runs/
helmsman-4b/
gguf/
*.gguf
