# Model card — `{adapter_name}`

A LoRA adapter for `{base_model}`, fine-tuned for `{task_summary}` using the [unhosted distillation recipe](https://github.com/unhosted-ai/unhosted-core/tree/main/models/distill).

## TL;DR

- **Base model:** `{base_model}` ({base_params} parameters)
- **Adapter type:** LoRA (r={lora_r}, α={lora_alpha}) — ~{trainable_params} trainable params
- **Training data:** {n_pairs} (prompt, response) pairs ({data_provenance})
- **Hardware used:** {training_hardware}
- **Training time:** {training_time}
- **License:** AGPL-3.0-or-later (matching base model's permissions plus daemon's terms)

## What this is good at

{intended_use}

## What this is NOT good at

{limitations}

Specifically, do not use this for:

- {antipattern_1}
- {antipattern_2}
- Anything safety-critical. This is a distillation; the failure modes of the base model carry through and any defects in the teacher's outputs are copied verbatim.

## Training recipe

1. Synthetic data generated by `gen_data.py` against `{teacher_model}` ({teacher_provenance}).
2. SFT loop in `train.py`: {epochs} epochs, lr={lr}, effective batch {effective_batch}, cosine schedule with {warmup}% warmup.
3. QLoRA: base loaded in 4-bit NF4, compute dtype {compute_dtype}, double quantization on. LoRA targets all attention + MLP projections.

The exact `data/train.jsonl` used is at `{data_artifact}`. The seed is `{seed}` so runs reproduce.

## Evaluation

| Metric | Base | This adapter |
| --- | --- | --- |
| {eval_metric_1} | {base_score_1} | {adapter_score_1} |
| {eval_metric_2} | {base_score_2} | {adapter_score_2} |

Eval set: {eval_set_description} ({n_eval} examples). The eval set is held out from training — the same generator script that built `data/train.jsonl` was run with `--seed` set to a different value and the resulting pairs were never seen by the trainer.

## Loading in unhosted

```bash
unhosted pull hf:{hf_repo}
unhosted serve --upstream-model {adapter_name}
```

(`unhosted pull` for custom adapters lands in v0.0.44; until then, point your existing inference stack at the adapter directly.)

## Reproducing this card

This file was generated from `model-card.template.md`. Fill in every `{placeholder}` before publishing — leaving a placeholder is a stronger signal than missing the section entirely.
